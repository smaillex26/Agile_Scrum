name: Test Devops

on:
  push:
    branches: [ main ]

jobs:
  build:
    build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: checkout code

      - name: Build 
        run: docker build -t my-app:${{GITHUB_SHA}} .

      - name: tests-unitaires
        run: |
          echo "lancements tests"
          docker run -d --name test-container my-app:${{GITHUB_SHA}}
          sleep 5
          docker exec test-container nginx -t echo "Tests unitaires réussis"
          docker stop test-container
      